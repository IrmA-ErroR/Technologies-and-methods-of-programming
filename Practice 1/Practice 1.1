#include <iostream>
#include <queue>

using namespace std;

struct Node
{
    int date;
    Node* next;

};

template <class Node>
class Queue
{
private:
    Node n;
public:
    virtyal ~Queue() {} // виртуальный деструктор для переопределения в реализациях
    
    bool empy() const { return n.empty(); }



};

int main()
{
    setlocale(LC_ALL, "rus");
    cout << "Поехали!\n";
    int number = 1;   
    cout << "Введите количество элементов: " << endl;
    cin >> number; 
    Node* node = new Node[number];
    for (int i = 0; i < number; i++)
    {
        node[i].date = rand() % 100;
    }

}
